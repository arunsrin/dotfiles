return {
  -- Add any other user configurations here

  plugins = {
    "user.plugins", -- This tells Lazy to look for plugin files in the 'user/plugins' directory
    -- You can add more plugin sources here if needed
  },

  -- Add any other configurations like key mappings, commands, etc.
}
